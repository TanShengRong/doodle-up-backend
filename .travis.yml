language: python
python:
  - "2.7"
  - "3.8"
# command to install dependencies
install:
  - pip install -r requirements.txt
before_script:
    - flake8 .
# command to run tests
# script:
    # - python -m flake8 # test for linting
    # - python -m pytest -v